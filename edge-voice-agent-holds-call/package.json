{
  "name": "edge-voice-agent-holds-call",
  "version": "1.0.0",
  "description": "Voice agent on Telnyx Edge Compute + Agent SDK — answers an inbound call and holds a conversation loop with STT → LLM → TTS, all in the same PoP. Zero-credential inference via the [telnyx] binding.",
  "main": "src/index.ts",
  "scripts": {
    "deploy": "telnyx-edge ship",
    "types": "telnyx-edge types"
  },
  "dependencies": {
    "@telnyx/edge-runtime": "^0.9.2"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  },
  "license": "MIT"
}

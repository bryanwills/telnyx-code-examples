{
  "name": "scheduled-reminder-agent",
  "version": "1.0.0",
  "description": "Scheduled SMS reminder agent on Telnyx Edge Compute + Agent SDK — sends timed reminders via zero-credential SMS, detects snooze intent via LLM, and adapts timing based on response patterns.",
  "main": "src/index.ts",
  "scripts": {
    "deploy": "telnyx-edge ship",
    "types": "telnyx-edge types"
  },
  "dependencies": {
    "@telnyx/edge-runtime": "^0.9.2"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  },
  "license": "MIT"
}

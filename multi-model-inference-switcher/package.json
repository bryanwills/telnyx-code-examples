{
  "name": "multi-model-inference-switcher",
  "version": "1.0.0",
  "description": "Multi-model inference switcher on Telnyx Edge Compute + Agent SDK — switch between LLM models at runtime via a KV feature flag. Zero-credential inference, no redeploy. Includes admin UI.",
  "main": "src/index.ts",
  "scripts": {
    "deploy": "telnyx-edge ship",
    "types": "telnyx-edge types"
  },
  "dependencies": {
    "@telnyx/edge-runtime": "^0.9.2"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  },
  "license": "MIT"
}
